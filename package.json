{
    "devDependencies": {
        "turbo": "^1.10.15"
    },
    "scripts": {
        "dev": "turbo run dev",
        "build": "turbo run build",
        "start": "node apps/server/dist/main",
        "typecheck": "turbo run typecheck",
        "format": "turbo run format",
        "lint": "turbo run lint",
        "seed": "cd apps/server && npx prisma db seed",
        "migrate:dev": "cd apps/server && npx prisma migrate dev",
        "migrate:deploy": "cd apps/server && npx prisma migrate deploy"
    },
    "workspaces": [
        "apps/*"
    ]
}
